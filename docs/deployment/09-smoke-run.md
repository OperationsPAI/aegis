# 09. End-to-end Fault Injection Smoke Run

This pass exercised the stack end-to-end on the local kind cluster `aegis-local`:
deploy a microservice demo, inject a network fault via Chaos Mesh, measure the
effect on user-visible latency, and verify clean recovery.

## Result summary

| Phase | avg (s) | p50 (s) | p95 (s) | min (s) | max (s) |
|------:|--------:|--------:|--------:|--------:|--------:|
| Baseline (n=20) | 0.0297 | 0.0135 | 0.0761 | 0.0104 | 0.0761 |
| During 200 ms NetworkChaos delay (n=20) | 2.7229 | 2.7198 | 2.7324 | 2.7153 | 2.7355 |
| Post-recovery (n=20) | 0.0240 | 0.0103 | 0.0700 | 0.0067 | 0.0918 |

Injection magnified a 200 ms per-packet delay to a ~2.7 s user-visible page
latency because the Online Boutique frontend fans out synchronously to
~10 backing services. Recovery was clean — post-injection latency matched
baseline within noise.

## Stack under test

- kind v1.34 cluster `aegis-local` (3 nodes, `control-plane` + 2 workers)
- Chaos Mesh `2.8.0` (Helm chart), `chaosDaemon.runtime=containerd`
- Target workload: Online Boutique `v0.10.2`
  ([`kubernetes-manifests.yaml`](https://raw.githubusercontent.com/GoogleCloudPlatform/microservices-demo/v0.10.2/release/kubernetes-manifests.yaml))
  deployed to namespace `demo`

## Reproduction

### 1. Deploy the demo

```bash
kubectl --context kind-aegis-local create namespace demo
kubectl --context kind-aegis-local -n demo apply -f \
  https://raw.githubusercontent.com/GoogleCloudPlatform/microservices-demo/v0.10.2/release/kubernetes-manifests.yaml
kubectl --context kind-aegis-local -n demo wait --for=condition=available --timeout=300s deployment --all
```

### 2. Port-forward the frontend

```bash
kubectl --context kind-aegis-local -n demo port-forward svc/frontend 28080:80 &
```

Note: pick a host port that is free on BOTH IPv4 and IPv6 loopback.
`kubectl port-forward` binds `[::1]`; if a daemon (e.g. flutter) is already
on `127.0.0.1:<port>`, curl against `127.0.0.1` hits the wrong process
silently. `28080` was free in this pass; `18080` was not.

### 3. Baseline

```bash
for i in $(seq 1 20); do
  curl -s -o /dev/null --http1.1 -H "Connection: close" \
    -w "%{time_total}\n" http://127.0.0.1:28080/
done
```

### 4. Apply the fault

`docs/deployment/networkchaos-smoke.yaml` in the repo is the spec
previously generated by the `aegisctl chaos network delay --dry-run` path.
For this pass, the equivalent raw NetworkChaos CRD was applied directly:

```yaml
apiVersion: chaos-mesh.org/v1alpha1
kind: NetworkChaos
metadata:
  name: frontend-delay-smoke
  namespace: demo
spec:
  action: delay
  mode: all
  selector:
    namespaces:
      - demo
    labelSelectors:
      app: frontend
  delay:
    latency: "200ms"
    correlation: "0"
    jitter: "0ms"
  direction: to
  duration: "90s"
```

```bash
kubectl --context kind-aegis-local apply -f /tmp/networkchaos-demo-frontend.yaml
```

Confirm injection:

```bash
kubectl --context kind-aegis-local -n demo get networkchaos frontend-delay-smoke \
  -o jsonpath='{.status.conditions}'
# ...{"status":"True","type":"AllInjected"}...
```

### 5. Curl during injection (same loop as baseline), then clean up

```bash
kubectl --context kind-aegis-local delete networkchaos -n demo frontend-delay-smoke
```

Final observed latency stats are in the table at the top of this page.

## aegisctl path — status

`aegisctl chaos network delay --namespace demo --app frontend ...` against a
live backend returned HTTP 500 because the backend rejects arbitrary
namespaces — the `pedestal/namespace` field is validated against a fixed set
of registered benchmark systems (`ts`, `hs`, `sn`, `ob`, `media`,
`otel-demo`). Using `--dry-run` still produces a correct `FaultSpec` YAML
that a pedestal-aware backend would translate into the CRD applied above.

Follow-up for `aegisctl chaos` to work end-to-end in this setup: either
register the `demo` namespace with the backend's benchmark registry, or
reshape the submit endpoint so an ad-hoc kind namespace does not require a
pre-declared benchmark. Tracking under `#10`.

## Known gaps hit in this pass

### Chaos Mesh default runtime on kind is wrong

**Where**: 02-chaos-mesh.md default install path in issue `#7`
**Symptom**: NetworkChaos reconcile stuck on `unable to flush ip sets for
pod ... : expected docker:// but got container`
**Root cause**: `chaosDaemon.runtime` defaults to `docker`; kind v1.34 uses
`containerd`, so the chaos-daemon's docker client rejects the containerd
container ID
**Workaround applied**: `helm upgrade chaos-mesh chaos-mesh/chaos-mesh
--reuse-values --set chaosDaemon.runtime=containerd --set
chaosDaemon.socketPath=/run/containerd/containerd.sock`
**Suggested fix (NOT applied here)**: follow-up issue title: `Default
Chaos Mesh install to containerd runtime for local kind deploys`; scope:
bake the runtime/socketPath flags into `02-chaos-mesh.md` and the
repo-owned values file so new kind installs work on first apply
