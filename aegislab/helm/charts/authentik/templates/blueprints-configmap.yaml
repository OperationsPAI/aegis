{{- if .Values.enabled }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ .Release.Name }}-authentik-blueprints
  labels:
    app.kubernetes.io/name: authentik
    app.kubernetes.io/instance: {{ .Release.Name }}
data:
  aegis-provider.yaml: |-
{{ .Files.Get "blueprints/aegis-provider.yaml" | indent 4 }}
{{- end }}
