# LEGACY — older "local" helm-values profile, superseded by manifests/kind

**Status as of 2026-05-12 (commit `17afc99`):** `rcabench.yaml`. The
validated cold-start runbook
([`docs/deployment/cold-start-kind.md`](../../../docs/deployment/cold-start-kind.md))
uses `manifests/kind/rcabench.yaml`, not this file. The two profiles
have drifted and `manifests/local/` has not been re-validated.
