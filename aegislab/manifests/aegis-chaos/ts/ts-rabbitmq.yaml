apiVersion: aegis-chaos/v1beta
kind: PointManifest
metadata:
  chart_version: "seed-genesis"
  instance: "seed"
  service: "ts-rabbitmq"
  system: "ts"
spec:
  replace_scope: "service"
  points:
    - capability: "container_kill"
      target:
        app: "ts-rabbitmq"
        container: "ts-rabbitmq"
        namespace: "ts"
    - capability: "cpu_stress"
      target:
        app: "ts-rabbitmq"
        container: "ts-rabbitmq"
        namespace: "ts"
    - capability: "memory_stress"
      target:
        app: "ts-rabbitmq"
        container: "ts-rabbitmq"
        namespace: "ts"
    - capability: "pod_failure"
      target:
        app: "ts-rabbitmq"
        namespace: "ts"
    - capability: "pod_kill"
      target:
        app: "ts-rabbitmq"
        namespace: "ts"
    - capability: "time_skew"
      target:
        app: "ts-rabbitmq"
        container: "ts-rabbitmq"
        namespace: "ts"
