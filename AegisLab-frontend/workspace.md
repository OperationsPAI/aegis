# Workspace 实现计划

## 需求理解 (Requirements Understanding)

### 核心概念

系统有**两套独立的侧边栏**：

```
┌─────────────────────────────────────────────────────────────────────────┐
│                         当前状态 (Projects 列表页)                        │
├─────────────────────────────────────────────────────────────────────────┤
│ ┌──────────┐  ┌────────────────────────────────────────────────────┐   │
│ │ 主侧边栏  │  │                                                    │   │
│ │          │  │                                                    │   │
│ │ 🏠 Home  │  │              Projects 列表                         │   │
│ │          │  │                                                    │   │
│ │ Projects │  │   ┌─────────────────────────────────────────────┐  │   │
│ │  • test  │  │   │ Name        │ Last Run  │ Visibility │ Runs │  │   │
│ │  • Agent │◄─┼───│ AgentLight  │ 2025-11-03│ Team      │  33  │  │   │
│ │  • rcage │  │   │ rcagent     │ 2025-11-05│ Team      │  14  │  │   │
│ │          │  │   └─────────────────────────────────────────────┘  │   │
│ │ Teams    │  │                                                    │   │
│ │  • cuhks │  │                                                    │   │
│ └──────────┘  └────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────────────────┘
                                    │
                                    │ 点击项目 "AgentLightning"
                                    ▼
┌─────────────────────────────────────────────────────────────────────────┐
│                      进入 Workspace 后 (主侧边栏隐藏)                     │
├─────────────────────────────────────────────────────────────────────────┤
│ ┌──────────┐  ┌────────────────────────────────────────────────────┐   │
│ │Workspace │  │ ☰ │ cuhkse > Projects > AgentLightning > Workspace │   │
│ │ 侧边栏   │  ├────────────────────────────────────────────────────┤   │
│ │          │  │                                                    │   │
│ │ 📋Project│  │  Workspace 内容区                                  │   │
│ │ 📊Worksp │  │                                                    │   │
│ │ 💉Inject │  │  ┌─────────────┐  ┌─────────────────────────────┐  │   │
│ │ ▶️Execut │  │  │ Runs 列表   │  │ Charts 面板                 │  │   │
│ │ 📈Evalua │  │  │             │  │                             │  │   │
│ │ 📄Report │  │  │ • run_001   │  │  📈 generation   📈 fitness │  │   │
│ │ 📦Artifa │  │  │ • run_002   │  │                             │  │   │
│ │          │  │  │ • run_003   │  │                             │  │   │
│ └──────────┘  │  └─────────────┘  └─────────────────────────────┘  │   │
│       ↑       └────────────────────────────────────────────────────┘   │
│       │                                                                 │
│    点击 ☰ 按钮可以切换回主侧边栏                                         │
└─────────────────────────────────────────────────────────────────────────┘
```

### 关键点

1. **主侧边栏 (MainLayout)** - 保持不变，用于全局导航
2. **Workspace 侧边栏** - 新写的，进入项目后显示，包含项目内部导航
3. **两者互斥** - 进入 Workspace 时主侧边栏隐藏，点击按钮可切换回来
4. **面包屑** - 在标题 "AegisLab RCA Benchmark Platform" 旁边显示导航路径

---

## 导航流程 (Navigation Flow)

```
主页面 (MainLayout + 主侧边栏可见)
    │
    ├── /home                    → 主侧边栏可见
    ├── /projects                → 主侧边栏可见
    ├── /teams/:teamName         → 主侧边栏可见
    │
    └── 点击项目列表中的项目
            │
            ▼
        进入 Workspace 布局 (主侧边栏隐藏)
            │
            ├── /:projectName              → Workspace侧边栏 (Project Overview)
            ├── /:projectName/workspace    → Workspace侧边栏 (Workspace)
            ├── /:projectName/injections   → Workspace侧边栏 (Injections)
            ├── /:projectName/executions   → Workspace侧边栏 (Executions)
            ├── /:projectName/evaluations  → Workspace侧边栏 (Evaluations)
            ├── /:projectName/reports      → Workspace侧边栏 (Reports)
            └── /:projectName/artifacts    → Workspace侧边栏 (Artifacts)
```

---

## 实现计划 (Implementation Plan)

### Phase 1: 布局系统重构

#### 1.1 创建 WorkspaceLayout 组件

新的布局组件，用于包裹所有 `/:projectName/*` 路由。

```typescript
// src/components/layout/WorkspaceLayout.tsx

interface WorkspaceLayoutProps {
  children: React.ReactNode;
}

功能:
- 独立于 MainLayout 的布局
- 自己的 Header (带面包屑)
- 自己的 Sidebar (Workspace 侧边栏)
- 按钮可以切换显示主侧边栏 (Drawer 形式)
```

**新文件**: `src/components/layout/WorkspaceLayout.tsx`
**复杂度**: Medium

#### 1.2 创建 WorkspaceSidebar 组件

```typescript
// src/components/layout/WorkspaceSidebar.tsx

菜单项:
- Project (项目概览) → /:projectName
- Workspace (工作区) → /:projectName/workspace
- ─────────────────
- Injections (故障注入) → /:projectName/injections
- Executions (执行记录) → /:projectName/executions
- ─────────────────
- Evaluations (评估) → /:projectName/evaluations [新增]
- Reports (报告) → /:projectName/reports [新增]
- ─────────────────
- Artifacts (产物) → /:projectName/artifacts

功能:
- 图标 + 文字
- 高亮当前页面
- 可折叠 (只显示图标)
```

**新文件**: `src/components/layout/WorkspaceSidebar.tsx`
**复杂度**: Low

#### 1.3 创建 WorkspaceHeader 组件

```typescript
// src/components/layout/WorkspaceHeader.tsx

结构:
┌────────────────────────────────────────────────────────────────────┐
│ [☰] │ 🔷 AegisLab │ cuhkse > Projects > AgentLightning > Workspace │
│     │             │                                                │
│     │             │                              [🔍] [🌙] [👤 admin]│
└────────────────────────────────────────────────────────────────────┘

功能:
- ☰ 汉堡菜单按钮 - 点击打开主侧边栏 (Drawer)
- Logo - 点击返回首页
- 面包屑导航 - Team > Projects > ProjectName > CurrentPage
- 右侧: 搜索、主题切换、用户菜单
```

**新文件**: `src/components/layout/WorkspaceHeader.tsx`
**复杂度**: Medium

#### 1.4 更新路由结构

```typescript
// src/App.tsx

// 方案: 使用不同的 Layout 组件

<Routes>
  {/* 公共路由 */}
  <Route path="/login" element={<Login />} />

  {/* 使用 MainLayout 的路由 (主侧边栏可见) */}
  <Route element={<MainLayout />}>
    <Route path="/home" element={<Home />} />
    <Route path="/projects" element={<ProjectList />} />
    <Route path="/projects/new" element={<ProjectEdit />} />
    <Route path="/teams/:teamName" element={<TeamDetail />} />
    <Route path="/profile" element={<Profile />} />
    {/* ... admin 路由 ... */}
  </Route>

  {/* 使用 WorkspaceLayout 的路由 (Workspace侧边栏) */}
  <Route path="/:projectName" element={<WorkspaceLayout />}>
    <Route index element={<ProjectOverview />} />
    <Route path="workspace" element={<ProjectWorkspace />} />
    <Route path="injections" element={<InjectionList />} />
    <Route path="injections/create" element={<InjectionCreate />} />
    <Route path="injections/:id" element={<InjectionDetail />} />
    <Route path="executions" element={<ExecutionList />} />
    <Route path="executions/new" element={<ExecutionCreate />} />
    <Route path="executions/:id" element={<ExecutionDetail />} />
    <Route path="evaluations" element={<EvaluationList />} />       {/* 新增 */}
    <Route path="evaluations/:id" element={<EvaluationDetail />} /> {/* 新增 */}
    <Route path="reports" element={<ReportList />} />               {/* 新增 */}
    <Route path="reports/new" element={<ReportEditor />} />         {/* 新增 */}
    <Route path="reports/:id" element={<ReportDetail />} />         {/* 新增 */}
    <Route path="artifacts" element={<ArtifactList />} />
    <Route path="settings" element={<ProjectSettings />} />
  </Route>
</Routes>
```

**修改文件**: `src/App.tsx`
**复杂度**: Medium

---

### Phase 2: Workspace 页面核心功能

#### 2.1 Workspace 页面结构

```typescript
// src/pages/projects/ProjectWorkspace.tsx

结构:
┌─────────────────────────────────────────────────────────────────┐
│ WorkspacePageHeader                                              │
│ [≡] 814750204's workspace [▼ Personal] ⚡ │ Saved 9 months ago  │
├──────────────────┬──────────────────────────────────────────────┤
│                  │ [🔍 Search panels...]  [⚙] [+Report] [+Panel]│
│  RunsPanel       │──────────────────────────────────────────────│
│  ┌────────────┐  │  ▼ Charts  3                    [◇] [⚙] [•••]│
│  │ Runs  233  │  │  ┌──────────┬──────────┬──────────┐         │
│  │ [<<][>>]   │  │  │generation│best_fit  │avg_fit   │         │
│  │ 🔍 Search  │  │  │   📈     │   📈     │   📈     │         │
│  │            │  │  └──────────┴──────────┴──────────┘         │
│  │ 👁🔵 run_1 │  │                                              │
│  │ 👁🔴 run_2 │  │  ▼ Media  1                     [◇] [⚙] [•••]│
│  │ 👁🟢 run_3 │  │  ┌────────────────────────────────┐          │
│  │ ...        │  │  │ ⓘ No data available...        │          │
│  │ 1-20 of233│  │  └────────────────────────────────┘          │
│  └────────────┘  │                                              │
└──────────────────┴──────────────────────────────────────────────┘
```

**修改文件**: `src/pages/projects/ProjectWorkspace.tsx`
**复杂度**: High

#### 2.2 WorkspacePageHeader 组件

```typescript
// src/components/workspace/WorkspacePageHeader.tsx

功能:
- 汉堡菜单 (展开/收起 RunsPanel)
- Workspace 名称 (可编辑)
- Workspace 选择器下拉菜单
- 保存状态显示
- 操作按钮 (Undo, Redo, Copy to my workspace)
```

**新文件**: `src/components/workspace/WorkspacePageHeader.tsx`
**复杂度**: Medium

#### 2.3 WorkspaceSelector 组件

```typescript
// src/components/workspace/WorkspaceSelector.tsx

下拉菜单结构:
┌─────────────────────────────┐
│ 🔍 Search workspaces        │
├─────────────────────────────┤
│ Personal workspaces  2      │
│   👤 Rainysteven1's workspace│
│      Saved on Jan 25...     │
│   [Show all]                │
├─────────────────────────────┤
│ 📑 No views yet             │
│    Save your workspace...   │
│    [Learn more about views] │
└─────────────────────────────┘
```

**新文件**: `src/components/workspace/WorkspaceSelector.tsx`
**复杂度**: Medium

#### 2.4 RunsPanel 组件

```typescript
// src/components/workspace/RunsPanel.tsx

功能:
- 标题栏: "Runs" + 数量 + 折叠按钮
- 搜索框
- 视图切换 (表格/卡片/排序)
- Run 列表
  - 可见性切换 (眼睛图标)
  - 状态颜色点
  - Run 名称
- 分页控制
- "NAME xxx visualized" 状态
```

**新文件**: `src/components/workspace/RunsPanel.tsx`
**复杂度**: High

#### 2.5 ChartsPanel 组件

```typescript
// src/components/workspace/ChartsPanel.tsx

功能:
- 搜索面板 (regex)
- 工具栏按钮
- Panel 分组 (Charts, Media, etc.)
- 拖拽排序
- 图表渲染
```

**新文件**: `src/components/workspace/ChartsPanel.tsx`
**复杂度**: High

#### 2.6 ChartCard 组件

```typescript
// src/components/workspace/ChartCard.tsx

功能:
- 图表标题
- 图例
- ECharts 渲染
- Tooltip 交互
```

**新文件**: `src/components/workspace/ChartCard.tsx`
**复杂度**: Medium

---

### Phase 3: 数据层

#### 3.1 Workspace 类型定义

```typescript
// src/types/workspace.ts

interface Workspace {
  id: string;
  name: string;
  project_id: string;
  owner_id: string;
  owner_type: 'user' | 'team';
  is_personal: boolean;
  settings: WorkspaceSettings;
  created_at: string;
  updated_at: string;
}

interface Run {
  id: string;
  name: string;
  status: 'running' | 'finished' | 'failed' | 'crashed';
  created_at: string;
  metrics: Record<string, number[]>;
  config: Record<string, unknown>;
}

// ... 更多类型
```

**新文件**: `src/types/workspace.ts`
**复杂度**: Low

#### 3.2 Workspace API Client

```typescript
// src/api/workspace.ts

// 如果后端 API 还没准备好，先用 Mock 数据
```

**新文件**: `src/api/workspace.ts`
**复杂度**: Medium

#### 3.3 Workspace Store

```typescript
// src/store/workspace.ts

interface WorkspaceStore {
  currentWorkspace: Workspace | null;
  selectedRuns: string[];
  visibleRuns: string[];
  panelLayout: PanelLayout[];
  // actions...
}
```

**新文件**: `src/store/workspace.ts`
**复杂度**: Medium

---

### Phase 4: 新页面

#### 4.1 Evaluations 页面

```typescript
// src/pages/evaluations/EvaluationList.tsx
// src/pages/evaluations/EvaluationDetail.tsx
```

**新文件**: `src/pages/evaluations/`
**复杂度**: Medium

#### 4.2 Reports 页面

```typescript
// src/pages/reports/ReportList.tsx
// src/pages/reports/ReportDetail.tsx
// src/pages/reports/ReportEditor.tsx
```

**新文件**: `src/pages/reports/`
**复杂度**: Medium-High

---

## 文件清单 (File Checklist)

### 新增文件

| 文件路径                                           | 描述               | 优先级 |
| -------------------------------------------------- | ------------------ | ------ |
| `src/components/layout/WorkspaceLayout.tsx`        | Workspace 布局容器 | P0     |
| `src/components/layout/WorkspaceLayout.css`        | 布局样式           | P0     |
| `src/components/layout/WorkspaceSidebar.tsx`       | Workspace 侧边栏   | P0     |
| `src/components/layout/WorkspaceSidebar.css`       | 侧边栏样式         | P0     |
| `src/components/layout/WorkspaceHeader.tsx`        | Workspace 顶部栏   | P0     |
| `src/components/layout/WorkspaceHeader.css`        | 顶部栏样式         | P0     |
| `src/components/workspace/WorkspacePageHeader.tsx` | 页面头部           | P1     |
| `src/components/workspace/WorkspaceSelector.tsx`   | 工作区选择器       | P1     |
| `src/components/workspace/RunsPanel.tsx`           | Runs 列表面板      | P1     |
| `src/components/workspace/RunsPanel.css`           | Runs 面板样式      | P1     |
| `src/components/workspace/ChartsPanel.tsx`         | 图表面板           | P1     |
| `src/components/workspace/ChartsPanel.css`         | 图表面板样式       | P1     |
| `src/components/workspace/ChartCard.tsx`           | 单个图表卡片       | P1     |
| `src/types/workspace.ts`                           | 类型定义           | P0     |
| `src/api/workspace.ts`                             | API Client         | P1     |
| `src/store/workspace.ts`                           | 状态管理           | P1     |
| `src/pages/evaluations/EvaluationList.tsx`         | 评估列表           | P2     |
| `src/pages/evaluations/EvaluationDetail.tsx`       | 评估详情           | P2     |
| `src/pages/reports/ReportList.tsx`                 | 报告列表           | P2     |
| `src/pages/reports/ReportDetail.tsx`               | 报告详情           | P2     |
| `src/pages/reports/ReportEditor.tsx`               | 报告编辑器         | P2     |

### 修改文件

| 文件路径                                  | 修改内容     | 优先级 |
| ----------------------------------------- | ------------ | ------ |
| `src/App.tsx`                             | 路由结构调整 | P0     |
| `src/pages/projects/ProjectWorkspace.tsx` | 重构页面内容 | P1     |

### 删除文件 (可选)

| 文件路径                                   | 原因                     |
| ------------------------------------------ | ------------------------ |
| `src/components/layout/ProjectLayout.tsx`  | 被 WorkspaceLayout 替代  |
| `src/components/layout/ProjectSidebar.tsx` | 被 WorkspaceSidebar 替代 |

---

## 实现顺序 (Implementation Order)

```
Step 1: 布局基础 (P0)
├── WorkspaceLayout.tsx
├── WorkspaceSidebar.tsx
├── WorkspaceHeader.tsx
└── 更新 App.tsx 路由

Step 2: Workspace 页面 (P1)
├── types/workspace.ts
├── WorkspacePageHeader.tsx
├── RunsPanel.tsx
├── ChartsPanel.tsx
└── 重构 ProjectWorkspace.tsx

Step 3: 数据层 (P1)
├── api/workspace.ts (Mock 数据)
└── store/workspace.ts

Step 4: 新页面 (P2)
├── Evaluations 页面
└── Reports 页面
```

---

## UI 对比参考

### W&B 原版 (第3张图)

```
┌─────────────────────────────────────────────────────────────────┐
│ cuhkse > Projects > pandora-ga > Workspace                      │
├──────┬──────────────────────────────────────────────────────────┤
│      │ [≡] 814750204's workspace [Personal workspace] ⚡       │
│ Proj │ ────────────────────────────────────────────────────────│
│ Work │ Runs 233 [<<][>>]  │ [Search panels...] [⚙][+Rep][+Pan] │
│ Runs │ ─────────────────  │ ▼ Charts 3                         │
│ Jobs │ 🔍 Search runs     │ ┌────────┬────────┬────────┐       │
│ Auto │ ⊞ ≡ ↕ ≡           │ │generat │best_fit│avg_fit │       │
│ Swee │ ⊙ NAME 233 viz    │ │  📈    │  📈    │  📈    │       │
│ Repo │ 👁🔵 ga_run_001   │ └────────┴────────┴────────┘       │
│ Arti │ 👁🔴 ga_run_002   │ ▼ Media 1                          │
│      │ 👁🟢 ga_run_003   │ [No data available]                 │
│      │ 1-20 of 233       │                                     │
└──────┴──────────────────────────────────────────────────────────┘
```

### AegisLab 目标版本

```
┌─────────────────────────────────────────────────────────────────┐
│ [☰] 🔷 AegisLab │ Team > Projects > AgentLightning > Workspace  │
├──────┬──────────────────────────────────────────────────────────┤
│      │ [≡] user's workspace [Personal] ⚡  │ Saved 2 hours ago  │
│ Proj │ ────────────────────────────────────────────────────────│
│ Work │ Runs 33 [<<][>>]   │ [Search panels...] [⚙][+Rep][+Pan] │
│ ──── │ ─────────────────  │ ▼ Charts 3                         │
│ Inje │ 🔍 Search runs     │ ┌────────┬────────┬────────┐       │
│ Exec │ ⊞ ≡ ↕ ≡           │ │metric_1│metric_2│metric_3│       │
│ ──── │ ⊙ NAME 33 viz     │ │  📈    │  📈    │  📈    │       │
│ Eval │ 👁🔵 exec_001     │ └────────┴────────┴────────┘       │
│ Repo │ 👁🔴 exec_002     │ ▼ Media 1                          │
│ ──── │ 👁🟢 exec_003     │ [No data available]                 │
│ Arti │ 1-20 of 33        │                                     │
└──────┴──────────────────────────────────────────────────────────┘

点击左上角 [☰] 按钮:
┌───────────────────┐
│ 🏠 Home           │  ← 主侧边栏以 Drawer 形式弹出
│                   │
│ Projects          │
│  • test           │
│  • AgentLightning │
│  • rcagent        │
│                   │
│ Teams             │
│  • cuhkse         │
└───────────────────┘
```

---

## 待确认问题 (Questions)

1. **后端 API** - Workspace 相关的 API 是否已经存在？还是需要 Mock？
2. **Runs 数据源** - 是用 Executions 还是有独立的 Runs 概念？
3. **图表数据** - 指标数据从哪里获取？Execution 的 metrics？
4. **权限** - Personal workspace vs Team workspace 的权限区分？

---

## Phase 5: Injections 页面设计 (W&B Table 风格)

### 5.1 Injections 页面整体结构

基于 W&B Runs Table 设计，Injections 页面采用全宽表格布局：

```
┌─────────────────────────────────────────────────────────────────────────────┐
│ Injections  33                                                         [«] │
├─────────────────────────────────────────────────────────────────────────────┤
│ ┌─────────────────────────────────────────────────────────────────────────┐ │
│ │ 🔍 Search injections                                                    │ │
│ └─────────────────────────────────────────────────────────────────────────┘ │
│ ┌──────────┬─────────┬──────────┬──────────────┐    ┌──────┬──────┬──────┐ │
│ │ ≡ Filter │ ⊞ Group │ ↕ Sort   │ + New Inject │    │  ⬇   │  ✨  │ ≡Col │ │
│ └──────────┴─────────┴──────────┴──────────────┘    └──────┴──────┴──────┘ │
├─────────────────────────────────────────────────────────────────────────────┤
│ ☐ │ 👁 │ ● │ NAME              │ NOTES  │ USER │ TAGS │ CREATED │ STATUS ... │
│───┼───┼───┼───────────────────┼────────┼──────┼──────┼─────────┼────────────│
│ ☐ │ 👁 │ 🔵 │ network_delay_001 │ Add... │ admin│ prod │ 3mo ago │ Success    │
│ ☐ │ 👁 │ 🟢 │ cpu_stress_002    │ Add... │ admin│ test │ 3mo ago │ Running    │
│ ☐ │ 👁 │ 🔴 │ memory_leak_003   │ Add... │ dev  │      │ 3mo ago │ Failed     │
│ ☐ │ 👁 │ 🟡 │ disk_io_004       │ Add... │ admin│ prod │ 3mo ago │ Pending    │
│ ...                                                                         │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                          1-20 ▼  of 33  < > │
└─────────────────────────────────────────────────────────────────────────────┘
```

### 5.2 Injections 表格列定义

#### 固定列 (Pinned Columns)

| 列名 | 字段       | 宽度  | 说明                            |
| ---- | ---------- | ----- | ------------------------------- |
| ☐    | checkbox   | 40px  | 批量选择                        |
| 👁   | visibility | 40px  | 可视化开关 (眼睛图标)           |
| ●    | status_dot | 40px  | 状态颜色点                      |
| NAME | name       | 200px | 注入名称 + "33 visualized" 状态 |

#### 可见列 (Visible & Pinned)

| 列名       | 字段             | 类型     | 说明                                      |
| ---------- | ---------------- | -------- | ----------------------------------------- |
| Name       | `name`           | string   | 注入名称，带锁定+固定图标                 |
| Notes      | `notes`          | string   | 备注，可点击添加 "Add notes..."           |
| User       | `user`           | string   | 创建用户                                  |
| Tags       | `tags`           | string[] | 标签数组                                  |
| Created    | `created_at`     | datetime | 创建时间，显示相对时间 "3mo ago"          |
| Status     | `state`          | enum     | 状态: Pending/Running/Success/Failed      |
| Fault Type | `fault_type`     | enum     | 故障类型: network/cpu/memory/disk/process |
| Category   | `category`       | string   | 故障分类                                  |
| Target     | `benchmark_name` | string   | 目标服务/容器                             |
| Pedestal   | `pedestal_name`  | string   | 测试环境                                  |
| Duration   | `pre_duration`   | number   | 持续时间 (秒)                             |
| Runtime    | `runtime`        | string   | 实际运行时间 "2m 43s"                     |

#### 隐藏列 (Hidden)

| 列名         | 字段           | 说明         |
| ------------ | -------------- | ------------ |
| ID           | `id`           | 注入ID       |
| Description  | `description`  | 详细描述     |
| Updated      | `updated_at`   | 更新时间     |
| Start Time   | `start_time`   | 开始时间     |
| End Time     | `end_time`     | 结束时间     |
| Task ID      | `task_id`      | 关联任务ID   |
| Benchmark ID | `benchmark_id` | 基准容器ID   |
| Pedestal ID  | `pedestal_id`  | 测试环境ID   |
| Labels       | `labels`       | 完整标签对象 |
| Ground Truth | `ground_truth` | 预期结果     |

### 5.3 Injections TypeScript 类型

```typescript
// src/types/workspace.ts (追加)

interface InjectionTableRow {
  id: number;
  name: string;
  notes?: string;
  user: string;
  tags: string[];
  created_at: string;
  updated_at: string;
  state: InjectionState;
  fault_type: FaultType;
  category: string;
  benchmark_name: string;
  pedestal_name: string;
  pre_duration: number;
  runtime?: string;
  // 隐藏字段
  description?: string;
  start_time?: string;
  end_time?: string;
  task_id?: string;
  benchmark_id?: number;
  pedestal_id?: number;
  labels?: LabelItem[];
  ground_truth?: GroundTruth[];
}

type InjectionState = 'pending' | 'running' | 'success' | 'failed' | 'stopped';
type FaultType =
  | 'network'
  | 'cpu'
  | 'memory'
  | 'disk'
  | 'process'
  | 'kubernetes';

// 列配置
interface ColumnConfig {
  key: string;
  title: string;
  dataIndex: string;
  width?: number | string;
  visible: boolean;
  pinned: boolean;
  order: number;
}

// 表格视图设置 (持久化到 localStorage)
interface TableViewSettings {
  columns: ColumnConfig[];
  sortBy?: string;
  sortOrder?: 'asc' | 'desc';
  filters?: Record<string, unknown>;
  pageSize: number;
}
```

---

## Phase 6: Evaluations 页面设计 (W&B Table 风格)

### 6.1 Evaluations 页面整体结构

```
┌─────────────────────────────────────────────────────────────────────────────┐
│ Evaluations  28                                                        [«] │
├─────────────────────────────────────────────────────────────────────────────┤
│ ┌─────────────────────────────────────────────────────────────────────────┐ │
│ │ 🔍 Search evaluations                                                   │ │
│ └─────────────────────────────────────────────────────────────────────────┘ │
│ ┌──────────┬─────────┬──────────┬─────────────┐    ┌──────┬──────┬──────┐  │
│ │ ≡ Filter │ ⊞ Group │ ↕ Sort   │ + New Eval  │    │  ⬇   │  ✨  │ ≡Col │  │
│ └──────────┴─────────┴──────────┴─────────────┘    └──────┴──────┴──────┘  │
├─────────────────────────────────────────────────────────────────────────────┤
│ ☐ │ 👁 │ ● │ NAME              │ NOTES │ USER │ CREATED │ SCORE │ MODEL ... │
│───┼───┼───┼───────────────────┼───────┼──────┼─────────┼───────┼───────────│
│ ☐ │ 👁 │ 🔵 │ eval_spider_001   │ Add...│ admin│ 3mo ago │ 0.847 │ gpt-4     │
│ ☐ │ 👁 │ 🟢 │ eval_spider_002   │ Add...│ admin│ 3mo ago │ 0.923 │ claude-3  │
│ ☐ │ 👁 │ 🟡 │ eval_spider_003   │ Add...│ dev  │ 3mo ago │   -   │ gemini    │
│ ☐ │ 👁 │ 🔴 │ eval_spider_004   │ Add...│ admin│ 3mo ago │ 0.156 │ gpt-3.5   │
│ ...                                                                         │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                          1-20 ▼  of 28  < > │
└─────────────────────────────────────────────────────────────────────────────┘
```

### 6.2 Evaluations 表格列定义

#### 固定列 (Pinned Columns)

| 列名 | 字段       | 宽度  | 说明                            |
| ---- | ---------- | ----- | ------------------------------- |
| ☐    | checkbox   | 40px  | 批量选择                        |
| 👁   | visibility | 40px  | 可视化开关                      |
| ●    | status_dot | 40px  | 状态颜色点                      |
| NAME | name       | 200px | 评估名称 + "28 visualized" 状态 |

#### 可见列 (Visible & Pinned)

| 列名      | 字段             | 类型     | 说明                                   |
| --------- | ---------------- | -------- | -------------------------------------- |
| Name      | `name`           | string   | 评估名称                               |
| Notes     | `notes`          | string   | 备注 "Add notes..."                    |
| User      | `user`           | string   | 创建用户                               |
| Tags      | `tags`           | string[] | 标签数组                               |
| Created   | `created_at`     | datetime | 创建时间 "3mo ago"                     |
| Status    | `state`          | enum     | 状态: Pending/Running/Completed/Failed |
| Runtime   | `runtime`        | string   | 运行时间 "18m 49s"                     |
| Score     | `score`          | number   | 综合评分 0.0-1.0                       |
| Model     | `model`          | string   | 使用的模型名称                         |
| Algorithm | `algorithm`      | string   | 算法名称                               |
| Execution | `execution_name` | string   | 关联的执行名称                         |
| Injection | `injection_name` | string   | 关联的注入名称                         |
| Precision | `precision`      | number   | 精确率                                 |
| Recall    | `recall`         | number   | 召回率                                 |
| F1        | `f1_score`       | number   | F1分数                                 |

#### 隐藏列 (Hidden)

| 列名         | 字段           | 说明         |
| ------------ | -------------- | ------------ |
| ID           | `id`           | 评估ID       |
| Description  | `description`  | 详细描述     |
| Updated      | `updated_at`   | 更新时间     |
| Start Time   | `start_time`   | 开始时间     |
| End Time     | `end_time`     | 结束时间     |
| Execution ID | `execution_id` | 关联执行ID   |
| Injection ID | `injection_id` | 关联注入ID   |
| Config       | `config`       | 评估配置JSON |
| Metrics      | `metrics`      | 完整指标对象 |
| Labels       | `labels`       | 标签对象     |

### 6.3 Evaluations TypeScript 类型

```typescript
// src/types/workspace.ts (追加)

interface EvaluationTableRow {
  id: number;
  name: string;
  notes?: string;
  user: string;
  tags: string[];
  created_at: string;
  updated_at: string;
  state: EvaluationState;
  runtime?: string;
  // 评估指标
  score?: number;
  precision?: number;
  recall?: number;
  f1_score?: number;
  // 关联信息
  model: string;
  algorithm: string;
  execution_id?: number;
  execution_name?: string;
  injection_id?: number;
  injection_name?: string;
  // 隐藏字段
  description?: string;
  start_time?: string;
  end_time?: string;
  config?: Record<string, unknown>;
  metrics?: Record<string, number>;
  labels?: LabelItem[];
}

type EvaluationState = 'pending' | 'running' | 'completed' | 'failed';
```

---

## Phase 7: Manage Columns 模态框设计

### 7.1 模态框结构 (参考 W&B)

```
┌─────────────────────────────────────────────────────────────────┐
│ Manage columns                                              [X] │
├─────────────────────────────────────────────────────────────────┤
│ Configure columns to display in the table. Pinned columns       │
│ persist when the table is collapsed.                            │
├─────────────────────────────────────────────────────────────────┤
│ ┌─────────────────────────────────────────────────────────────┐ │
│ │ 🔍 Search columns                              [Fuzzy ▼]    │ │
│ └─────────────────────────────────────────────────────────────┘ │
├────────────────────────────┬────────────────────────────────────┤
│ HIDDEN  11                 │ VISIBLE & PINNED  372              │
│ [Show all ▼]               │ [Hide unpinned ◄]                  │
├────────────────────────────┼────────────────────────────────────┤
│ ≡ ID                       │ Aa Name                    🔒 📌   │
│ ≡ Group                    │ ≡  Notes                           │
│ ≡ Job Type                 │ 👤 User                            │
│ ≡ Updated                  │ 🏷  Tags                            │
│ ≡ End Time                 │ 📅 Created                         │
│ ≡ Hostname                 │ ⏱  Runtime                         │
│ ≡ Description              │ ≡  Sweep                           │
│ ≡ Commit                   │ ≡  actor_rollout_ref.actor.check.. │
│ ≡ GitHub                   │ ≡  actor_rollout_ref.actor.check.. │
│ ≡ GPU Count                │    ...                             │
│                            │                                    │
├────────────────────────────┴────────────────────────────────────┤
│                                                         [Close] │
└─────────────────────────────────────────────────────────────────┘
```

### 7.2 交互功能

1. **拖拽排序**: 列可以在 HIDDEN 和 VISIBLE 之间拖拽
2. **搜索过滤**: 支持 Fuzzy/Exact 两种搜索模式
3. **批量操作**: "Show all" / "Hide unpinned" 按钮
4. **固定列**: 点击 📌 图标固定列，固定的列显示 🔒
5. **列图标**: 根据数据类型显示不同图标
   - `Aa` 文本
   - `#` 数字
   - `📅` 日期
   - `👤` 用户
   - `🏷` 标签

### 7.3 ColumnManager 组件设计

```typescript
// src/components/workspace/ColumnManager.tsx

interface ColumnManagerProps {
  open: boolean;
  onClose: () => void;
  columns: ColumnConfig[];
  onColumnsChange: (columns: ColumnConfig[]) => void;
}

interface ColumnConfig {
  key: string;
  title: string;
  dataIndex: string;
  type: 'text' | 'number' | 'date' | 'user' | 'tags' | 'status';
  visible: boolean;
  pinned: boolean;
  locked?: boolean;  // Name 列锁定不可隐藏
  order: number;
}

// 组件结构
<Modal
  title="Manage columns"
  open={open}
  onCancel={onClose}
  footer={<Button onClick={onClose}>Close</Button>}
  width={700}
>
  <Text type="secondary">
    Configure columns to display in the table. Pinned columns persist
    when the table is collapsed.
  </Text>

  <Input.Search placeholder="Search columns" suffix={<Select defaultValue="Fuzzy" />} />

  <Row gutter={16}>
    {/* HIDDEN 列 */}
    <Col span={12}>
      <div className="column-section">
        <div className="section-header">
          <Text strong>HIDDEN</Text>
          <Badge count={hiddenColumns.length} />
          <Button type="link">Show all</Button>
        </div>
        <DragDropContext>
          <Droppable droppableId="hidden">
            {hiddenColumns.map(col => (
              <ColumnItem key={col.key} column={col} />
            ))}
          </Droppable>
        </DragDropContext>
      </div>
    </Col>

    {/* VISIBLE & PINNED 列 */}
    <Col span={12}>
      <div className="column-section">
        <div className="section-header">
          <Text strong>VISIBLE & PINNED</Text>
          <Badge count={visibleColumns.length} />
          <Button type="link">Hide unpinned</Button>
        </div>
        <DragDropContext>
          <Droppable droppableId="visible">
            {visibleColumns.map(col => (
              <ColumnItem
                key={col.key}
                column={col}
                showPinButton
                showLockIcon={col.locked}
              />
            ))}
          </Droppable>
        </DragDropContext>
      </div>
    </Col>
  </Row>
</Modal>
```

### 7.4 列类型图标映射

```typescript
const columnTypeIcons: Record<string, React.ReactNode> = {
  text: <FontSizeOutlined />,      // Aa
  number: <NumberOutlined />,       // #
  date: <CalendarOutlined />,       // 📅
  user: <UserOutlined />,           // 👤
  tags: <TagsOutlined />,           // 🏷
  status: <CheckCircleOutlined />,  // ✓
  default: <MenuOutlined />,        // ≡
};
```

---

## Phase 8: 通用 WorkspaceTable 组件

### 8.1 组件设计

为 Injections、Evaluations、Runs 等表格提供统一的表格组件：

```typescript
// src/components/workspace/WorkspaceTable.tsx

interface WorkspaceTableProps<T> {
  // 数据
  dataSource: T[];
  loading: boolean;

  // 列配置
  columns: ColumnConfig[];
  onColumnsChange: (columns: ColumnConfig[]) => void;

  // 选择
  selectedRowKeys: React.Key[];
  onSelectChange: (keys: React.Key[]) => void;
  visualizedRowKeys: React.Key[];
  onVisualizeChange: (keys: React.Key[]) => void;

  // 分页
  pagination: {
    current: number;
    pageSize: number;
    total: number;
  };
  onPaginationChange: (page: number, size: number) => void;

  // 搜索/筛选
  searchText: string;
  onSearchChange: (text: string) => void;
  filters: Record<string, unknown>;
  onFiltersChange: (filters: Record<string, unknown>) => void;

  // 操作
  onRowClick?: (record: T) => void;
  onNewClick?: () => void;
  newButtonText?: string;

  // 存储 key (localStorage)
  storageKey: string;
}
```

### 8.2 表格头部工具栏

```
┌─────────────────────────────────────────────────────────────────────────────┐
│ 🔍 Search injections                                                        │
├─────────────────────────────────────────────────────────────────────────────┤
│ [≡ Filter] [⊞ Group] [↕ Sort] [+ New Injection]        [⬇] [✨] [≡ Columns] │
└─────────────────────────────────────────────────────────────────────────────┘

- Filter: 打开筛选面板
- Group: 分组选项 (按 fault_type, status, user 等)
- Sort: 排序选项
- New xxx: 新建按钮
- ⬇: 导出 CSV
- ✨: 高级设置
- Columns: 打开 Manage columns 模态框
```

### 8.3 表格底部分页

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                                                    1-20 [▼]  of 33   [<] [>]│
└─────────────────────────────────────────────────────────────────────────────┘

- 1-20: 当前显示范围
- [▼]: 每页条数选择 (10/20/50/100)
- of 33: 总数
- [<] [>]: 上一页/下一页
```

---

## Phase 9: 状态颜色定义

### 9.1 Injection 状态颜色

| 状态    | 颜色 | 色值    |
| ------- | ---- | ------- |
| Pending | 黄色 | #faad14 |
| Running | 蓝色 | #1890ff |
| Success | 绿色 | #52c41a |
| Failed  | 红色 | #f5222d |
| Stopped | 灰色 | #8c8c8c |

### 9.2 Evaluation 状态颜色

| 状态      | 颜色 | 色值    |
| --------- | ---- | ------- |
| Pending   | 黄色 | #faad14 |
| Running   | 蓝色 | #1890ff |
| Completed | 绿色 | #52c41a |
| Failed    | 红色 | #f5222d |

### 9.3 CSS 变量

```css
/* src/styles/workspace-table.css */

:root {
  --status-pending: #faad14;
  --status-running: #1890ff;
  --status-success: #52c41a;
  --status-completed: #52c41a;
  --status-failed: #f5222d;
  --status-stopped: #8c8c8c;
}

.status-dot {
  width: 8px;
  height: 8px;
  border-radius: 50%;
  display: inline-block;
}

.status-dot--pending {
  background-color: var(--status-pending);
}
.status-dot--running {
  background-color: var(--status-running);
}
.status-dot--success {
  background-color: var(--status-success);
}
.status-dot--completed {
  background-color: var(--status-completed);
}
.status-dot--failed {
  background-color: var(--status-failed);
}
.status-dot--stopped {
  background-color: var(--status-stopped);
}
```

---

## 文件清单更新 (File Checklist Update)

### 新增文件 (Phase 5-9)

| 文件路径                                       | 描述                | 优先级 |
| ---------------------------------------------- | ------------------- | ------ |
| `src/components/workspace/WorkspaceTable.tsx`  | 通用表格组件        | P1     |
| `src/components/workspace/WorkspaceTable.css`  | 表格样式            | P1     |
| `src/components/workspace/ColumnManager.tsx`   | 列管理模态框        | P1     |
| `src/components/workspace/ColumnManager.css`   | 列管理样式          | P1     |
| `src/components/workspace/TableToolbar.tsx`    | 表格工具栏          | P1     |
| `src/components/workspace/TablePagination.tsx` | 表格分页            | P1     |
| `src/pages/projects/ProjectInjectionList.tsx`  | Injections 列表页   | P1     |
| `src/pages/projects/ProjectEvaluationList.tsx` | Evaluations 列表页  | P1     |
| `src/hooks/useTableColumns.ts`                 | 列配置 Hook         | P1     |
| `src/hooks/useTablePersistence.ts`             | 表格设置持久化 Hook | P1     |
| `src/styles/workspace-table.css`               | 表格全局样式        | P1     |

---

## 实现顺序更新

```
Step 1: 布局基础 (P0)
├── WorkspaceLayout.tsx
├── WorkspaceSidebar.tsx
├── WorkspaceHeader.tsx
└── 更新 App.tsx 路由

Step 2: 通用表格组件 (P1) ← 新增
├── WorkspaceTable.tsx
├── ColumnManager.tsx
├── TableToolbar.tsx
├── TablePagination.tsx
├── useTableColumns.ts
└── useTablePersistence.ts

Step 3: Injections 页面 (P1) ← 新增
└── ProjectInjectionList.tsx (使用 WorkspaceTable)

Step 4: Evaluations 页面 (P1) ← 新增
└── ProjectEvaluationList.tsx (使用 WorkspaceTable)

Step 5: Workspace 页面 (P1)
├── WorkspacePageHeader.tsx
├── RunsPanel.tsx (复用 WorkspaceTable)
├── ChartsPanel.tsx
└── 重构 ProjectWorkspace.tsx

Step 6: 数据层 (P1)
├── api/workspace.ts (Mock 数据)
└── store/workspace.ts
```

---

## Phase 10: W&B 风格表格增强 (Table Enhancements)

基于用户需求和 wandb 参考截图，需要实现以下4个功能：

### 10.1 可拖拽调整列宽 (Resizable Columns)

**需求**: 每个列可以通过拖拽边缘调整宽度，只有 pinned (固定) 的列不能调整。

**实现方案**: 自定义拖拽 handle

```
┌──────────────┬──────────────┬──────────────┐
│ NAME      ║│ NOTES     ║│ STATUS    ║│  ← 右侧 ║ 是拖拽手柄
├──────────────┼──────────────┼──────────────┤
│ injection_1  │ test note    │ Running      │
│ injection_2  │ ...          │ Finished     │
└──────────────┴──────────────┴──────────────┘
```

**修改文件**:
| 文件 | 修改内容 |
|------|----------|
| `src/components/workspace/WorkspaceTable.tsx` | 添加 resize handle 和拖拽逻辑 |
| `src/components/workspace/WorkspaceTable.css` | resize handle 样式 |
| `src/hooks/useColumnResize.ts` | 新建 Hook 处理拖拽状态 |

**代码设计**:

```typescript
// useColumnResize.ts
const useColumnResize = (
  columns: ColumnConfig[],
  onColumnsChange: (cols: ColumnConfig[]) => void
) => {
  const [resizing, setResizing] = useState<string | null>(null);

  const handleMouseDown = (columnKey: string, e: React.MouseEvent) => {
    const col = columns.find((c) => c.key === columnKey);
    if (col?.pinned) return; // pinned 列不可调整
    // ... 拖拽逻辑
  };

  return { resizing, handleMouseDown };
};
```

**CSS 样式**:

```css
.column-resize-handle {
  position: absolute;
  right: 0;
  top: 0;
  bottom: 0;
  width: 4px;
  cursor: col-resize;
  background: transparent;
}

.column-resize-handle:hover,
.column-resize-handle.resizing {
  background: var(--color-primary-500);
}
```

---

### 10.2 简化 Name 列 (Simplified Name Column)

**需求**: Name 列只显示名称文本，不要图标和彩色标签。

**当前实现** (ProjectInjectionList.tsx 第308-322行):

```tsx
// 当前 - 显示图标 + 名称 + fault_type 标签
const renderName = (record) => (
  <Space>
    <ExperimentOutlined style={{ color: faultTypeColors[record.fault_type] }} />
    <div>
      <Text strong>{record.name}</Text>
      <br />
      <Tag color={faultTypeColors[record.fault_type]}>{record.fault_type}</Tag>
    </div>
  </Space>
);
```

**简化后**:

```tsx
// 简化后 - 只显示名称
const renderName = (record: { name: string }) => (
  <Text strong>{record.name}</Text>
);
```

**修改文件**:
| 文件 | 修改内容 |
|------|----------|
| `src/pages/projects/ProjectInjectionList.tsx` | 简化 renderName 函数 |
| `src/pages/projects/ProjectExecutionList.tsx` | 简化 renderName 函数 |

---

### 10.3 批量操作按钮 (Bulk Actions Bar)

**需求**: 勾选行后显示批量操作按钮：删除、打标签、移动到项目。

**UI 设计**:

```
选中 3 行后显示:
┌─────────────────────────────────────────────────────────────────────────────┐
│ ☑ 3 selected  │  [🗑 Delete]  [🏷 Add Tags]  [📁 Move to Project]  │  [Clear]│
├─────────────────────────────────────────────────────────────────────────────┤
│ 表格内容...                                                                  │
└─────────────────────────────────────────────────────────────────────────────┘
```

**新建组件**: `BulkActionBar.tsx`

```typescript
// src/components/workspace/BulkActionBar.tsx

interface BulkActionBarProps {
  selectedCount: number;
  onDelete: () => void;
  onAddTags: () => void;
  onMoveToProject?: () => void;
  onClearSelection: () => void;
}

const BulkActionBar: React.FC<BulkActionBarProps> = ({
  selectedCount,
  onDelete,
  onAddTags,
  onMoveToProject,
  onClearSelection,
}) => (
  <div className="bulk-action-bar">
    <div className="bulk-action-info">
      <Checkbox checked indeterminate={false} />
      <Text strong>{selectedCount} selected</Text>
    </div>
    <Space>
      <Button danger icon={<DeleteOutlined />} onClick={onDelete}>
        Delete
      </Button>
      <Button icon={<TagsOutlined />} onClick={onAddTags}>
        Add Tags
      </Button>
      {onMoveToProject && (
        <Button icon={<FolderOutlined />} onClick={onMoveToProject}>
          Move to Project
        </Button>
      )}
    </Space>
    <Button type="text" onClick={onClearSelection}>
      Clear
    </Button>
  </div>
);
```

**WorkspaceTable 集成**:

```tsx
// WorkspaceTable.tsx 新增 Props
interface WorkspaceTableProps<T> {
  // ... 现有 props
  // 批量操作回调
  onBulkDelete?: () => void;
  onBulkAddTags?: () => void;
  onBulkMoveToProject?: () => void;
}

// 在 toolbar 下方渲染
{
  selectedRowKeys.length > 0 && (
    <BulkActionBar
      selectedCount={selectedRowKeys.length}
      onDelete={onBulkDelete}
      onAddTags={onBulkAddTags}
      onMoveToProject={onBulkMoveToProject}
      onClearSelection={() => onSelectChange([])}
    />
  );
}
```

**修改文件**:
| 文件 | 修改内容 |
|------|----------|
| `src/components/workspace/BulkActionBar.tsx` | 新建组件 |
| `src/components/workspace/BulkActionBar.css` | 样式 |
| `src/components/workspace/WorkspaceTable.tsx` | 添加批量操作 props 和渲染 |
| `src/pages/projects/ProjectInjectionList.tsx` | 添加批量操作 handlers |
| `src/pages/projects/ProjectExecutionList.tsx` | 添加批量操作 handlers |

---

### 10.4 NAME 列下拉菜单 (wandb 风格)

**需求**: NAME 列标题要有下拉菜单，和 wandb 一样的选项。

**wandb 下拉菜单选项** (参考第3张截图):

```
┌────────────────────────────┐
│ Make all visible           │  ← 让所有行可见 (眼睛开)
│ Make all hidden            │  ← 让所有行隐藏 (眼睛关)
├────────────────────────────┤
│ Only show visualized  [⌄]  │  ← 只显示可视化的行 (Toggle)
├────────────────────────────┤
│ Select visible runs        │  ← 选中所有可见的行
└────────────────────────────┘
```

**新建组件**: `NameColumnDropdown.tsx`

```typescript
// src/components/workspace/NameColumnDropdown.tsx

interface NameColumnDropdownProps {
  visualizedRowKeys: Key[];
  dataSource: { id: Key }[];
  onVisualizeChange: (keys: Key[]) => void;
  onSelectChange?: (keys: Key[]) => void;
  showOnlyVisualized: boolean;
  onShowOnlyVisualizedChange: (show: boolean) => void;
  visualizedCount: number;
  children: React.ReactNode;
}

const NameColumnDropdown: React.FC<NameColumnDropdownProps> = ({
  visualizedRowKeys,
  dataSource,
  onVisualizeChange,
  onSelectChange,
  showOnlyVisualized,
  onShowOnlyVisualizedChange,
  visualizedCount,
  children,
}) => {
  const menuItems: MenuProps['items'] = [
    {
      key: 'make-all-visible',
      icon: <EyeOutlined />,
      label: 'Make all visible',
      onClick: () => onVisualizeChange(dataSource.map(d => d.id)),
    },
    {
      key: 'make-all-hidden',
      icon: <EyeInvisibleOutlined />,
      label: 'Make all hidden',
      onClick: () => onVisualizeChange([]),
    },
    { type: 'divider' },
    {
      key: 'only-show-visualized',
      label: (
        <div className="dropdown-item-with-toggle">
          <span>Only show visualized</span>
          <Switch
            size="small"
            checked={showOnlyVisualized}
            onClick={(_, e) => e.stopPropagation()}
            onChange={(checked) => onShowOnlyVisualizedChange(checked)}
          />
        </div>
      ),
    },
    { type: 'divider' },
    {
      key: 'select-visible',
      icon: <CheckSquareOutlined />,
      label: 'Select visible runs',
      onClick: () => onSelectChange?.(visualizedRowKeys),
    },
  ];

  return (
    <Dropdown menu={{ items: menuItems }} trigger={['click']}>
      <div className="name-column-header">
        <EyeOutlined className="visualized-icon" />
        <span className="column-title">{children}</span>
        <Text type="secondary" className="visualized-count">
          {visualizedCount} visualized
        </Text>
        <DownOutlined className="dropdown-arrow" />
      </div>
    </Dropdown>
  );
};
```

**WorkspaceTable 集成**:

```tsx
// 新增 state
const [showOnlyVisualized, setShowOnlyVisualized] = useState(false);

// 过滤数据 (当 toggle 开启时)
const filteredData = useMemo(() => {
  if (showOnlyVisualized && visualizedRowKeys) {
    return dataSource.filter((d) => visualizedRowKeys.includes(d.id));
  }
  return dataSource;
}, [dataSource, showOnlyVisualized, visualizedRowKeys]);

// 在 Name 列使用 NameColumnDropdown
if (col.key === 'name') {
  column.title = (
    <NameColumnDropdown
      visualizedRowKeys={visualizedRowKeys || []}
      dataSource={dataSource}
      onVisualizeChange={onVisualizeChange}
      onSelectChange={onSelectChange}
      showOnlyVisualized={showOnlyVisualized}
      onShowOnlyVisualizedChange={setShowOnlyVisualized}
      visualizedCount={visualizedRowKeys?.length || 0}
    >
      {col.title}
    </NameColumnDropdown>
  );
}
```

**修改文件**:
| 文件 | 修改内容 |
|------|----------|
| `src/components/workspace/NameColumnDropdown.tsx` | 新建组件 |
| `src/components/workspace/NameColumnDropdown.css` | 样式 |
| `src/components/workspace/WorkspaceTable.tsx` | 集成下拉菜单和过滤逻辑 |

---

### 10.5 实现顺序

```
Phase 10 实现步骤:

Step 10.1: 简化 Name 列 (最简单)
├── 修改 ProjectInjectionList.tsx renderName
└── 修改 ProjectExecutionList.tsx renderName

Step 10.2: 批量操作按钮
├── 创建 BulkActionBar.tsx + CSS
├── 修改 WorkspaceTable.tsx 添加 props 和渲染
├── 修改 ProjectInjectionList.tsx 添加 handlers
└── 修改 ProjectExecutionList.tsx 添加 handlers

Step 10.3: NAME 列下拉菜单
├── 创建 NameColumnDropdown.tsx + CSS
├── 修改 WorkspaceTable.tsx 添加 state 和集成
└── 添加数据过滤逻辑

Step 10.4: 可拖拽调整列宽
├── 创建 useColumnResize.ts Hook
├── 修改 WorkspaceTable.tsx 添加 resize handles
└── 修改 WorkspaceTable.css 添加拖拽样式
```

---

### 10.6 文件清单

**新建文件**:
| 文件路径 | 描述 |
|---------|------|
| `src/components/workspace/BulkActionBar.tsx` | 批量操作栏组件 |
| `src/components/workspace/BulkActionBar.css` | 批量操作栏样式 |
| `src/components/workspace/NameColumnDropdown.tsx` | NAME列下拉菜单组件 |
| `src/components/workspace/NameColumnDropdown.css` | 下拉菜单样式 |
| `src/hooks/useColumnResize.ts` | 列宽调整 Hook |

**修改文件**:
| 文件路径 | 修改内容 |
|---------|----------|
| `src/components/workspace/WorkspaceTable.tsx` | 集成所有新功能 |
| `src/components/workspace/WorkspaceTable.css` | 添加 resize handle 样式 |
| `src/pages/projects/ProjectInjectionList.tsx` | 简化 renderName + 添加批量操作 |
| `src/pages/projects/ProjectExecutionList.tsx` | 简化 renderName + 添加批量操作 |

---

**等待确认**: 这个理解正确吗？可以开始实施吗？(yes/no/modify)
