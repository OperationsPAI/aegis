# AegisLab Profile & Projects 页面重构计划

## 需求概述

参考 Weights & Biases 设计，重构用户 Profile 页面和 Projects Tab，实现：

### Profile 页面（主页内容）

1. **左侧边栏**：头像、用户名、邮箱、Edit profile 按钮、Teams 列表
2. **主内容区 - Tabs**：
   - **Profile Tab**：Projects 展示、Activity 活跃度图（类似 GitHub 贡献图）、Recent Runs
   - **Projects Tab**：Starred Projects + 我创建的 Projects（无 Traces 列）
   - **Stars Tab**：收藏的项目列表

---

## 一、技术架构

### 1.1 新增 API 端点（需后端支持）

```typescript
// 用户活跃度数据
GET /api/v2/users/me/activity
Response: {
  contributions: Array<{date: string, count: number}>,  // 365天活跃数据
  total_runs: number,
  total_projects: number
}

// 用户最近 Runs（Executions）
GET /api/v2/users/me/recent-runs
Query: { limit: number }
Response: {
  items: Array<ExecutionResp>
}

// 收藏项目
POST /api/v2/projects/{id}/star
DELETE /api/v2/projects/{id}/star
GET /api/v2/users/me/starred-projects
```

### 1.2 前端状态管理

```typescript
// src/store/profile.ts - 新增 Zustand store
interface ProfileState {
  starredProjects: number[]; // 收藏的项目 ID 列表
  toggleStar: (projectId: number) => void;
  isStarred: (projectId: number) => boolean;
}
```

---

## 二、文件结构

### 2.1 新建文件

| 文件路径                                     | 用途                      |
| -------------------------------------------- | ------------------------- |
| `src/pages/profile/ProfilePage.tsx`          | Profile 主页面（带 Tabs） |
| `src/pages/profile/ProfilePage.css`          | Profile 页面样式          |
| `src/pages/profile/tabs/ProfileTab.tsx`      | Profile Tab 内容          |
| `src/pages/profile/tabs/ProjectsTab.tsx`     | Projects Tab 内容         |
| `src/pages/profile/tabs/StarsTab.tsx`        | Stars Tab 内容            |
| `src/components/profile/ProfileSidebar.tsx`  | Profile 左侧边栏          |
| `src/components/profile/ActivityGraph.tsx`   | GitHub 风格活跃度图       |
| `src/components/profile/RecentRuns.tsx`      | 最近运行列表              |
| `src/components/profile/ProjectCard.tsx`     | 项目卡片（带 star）       |
| `src/components/profile/StarredProjects.tsx` | 收藏项目区域              |
| `src/api/profile.ts`                         | Profile 相关 API          |
| `src/store/profile.ts`                       | Profile Zustand store     |

### 2.2 修改文件

| 文件路径                             | 修改内容                       |
| ------------------------------------ | ------------------------------ |
| `src/App.tsx`                        | 更新 /profile 路由到新页面     |
| `src/pages/projects/ProjectList.tsx` | 添加 star 功能列               |
| `src/types/api.ts`                   | 添加 Activity、RecentRuns 类型 |

---

## 三、组件详细设计

### 3.1 ProfilePage 布局

```
┌─────────────────────────────────────────────────────────────────────┐
│                           ProfilePage                               │
├───────────────┬─────────────────────────────────────────────────────┤
│               │  [ Profile ]  [ Projects ]  [ Stars ]               │
│   Sidebar     ├─────────────────────────────────────────────────────┤
│               │                                                     │
│   - Avatar    │   Tab Content Area                                  │
│   - Name      │                                                     │
│   - Username  │   Profile Tab:                                      │
│   - Email     │   ┌──────────────────────────────────────────────┐  │
│               │   │ Projects Section                             │  │
│   [Edit]      │   │ ┌─────┐ ┌─────┐ ┌─────┐                      │  │
│               │   │ │Card │ │Card │ │Card │                      │  │
│   Teams:      │   │ └─────┘ └─────┘ └─────┘                      │  │
│   - team1     │   └──────────────────────────────────────────────┘  │
│   - team2     │   ┌──────────────────────────────────────────────┐  │
│               │   │ Activity                                     │  │
│               │   │ ████ █ ██ ███ █ ██ ███ (GitHub 风格图)       │  │
│               │   └──────────────────────────────────────────────┘  │
│               │   ┌──────────────────────────────────────────────┐  │
│               │   │ Recent Runs                                  │  │
│               │   │ - Execution 1  |  2 mins ago  |  Success     │  │
│               │   │ - Execution 2  |  1 hour ago  |  Running     │  │
│               │   └──────────────────────────────────────────────┘  │
│               │                                                     │
└───────────────┴─────────────────────────────────────────────────────┘
```

### 3.2 Projects Tab 布局

```
┌─────────────────────────────────────────────────────────────────────┐
│ Your starred projects                                               │
├─────────────────────────────────────────────────────────────────────┤
│ ┌──────────────────────┐  ┌──────────────────────┐                  │
│ │ project-alpha    ⭐   │  │ project-beta     ⭐   │                  │
│ │ ○ Last updated 2m ago │  │ ○ Last updated 1h ago │                  │
│ │ 🔄 12 runs            │  │ 🔄 8 runs             │                  │
│ └──────────────────────┘  └──────────────────────┘                  │
├─────────────────────────────────────────────────────────────────────┤
│ Your projects                                    [+ New project]    │
├─────────────────────────────────────────────────────────────────────┤
│ 🔍 Search by project name...                        showing 5  < > │
├─────────────────────────────────────────────────────────────────────┤
│ Name              │ Last Run      │ Visibility │ Runs │ Star       │
├─────────────────────────────────────────────────────────────────────┤
│ my-project        │ 2 days ago    │ 🔒 Private │ 15   │ ⭐         │
│ another-project   │ 1 week ago    │ 👥 Team    │ 8    │ ☆          │
└─────────────────────────────────────────────────────────────────────┘
```

### 3.3 ActivityGraph 组件

参考 GitHub 贡献图设计：

- 显示近 365 天的活跃度
- 颜色深浅表示当天 Runs 数量
- 鼠标悬停显示具体日期和数量
- 使用 CSS Grid 或 SVG 实现

```typescript
interface ActivityData {
  date: string; // YYYY-MM-DD
  count: number; // 当天 runs 数量
}

// 颜色等级：0=无活动, 1-2=浅绿, 3-5=中绿, 6-10=深绿, >10=最深绿
```

---

## 四、实施步骤

### Phase 1: 基础架构 (P0)

**Step 1.1: 类型定义**

- [ ] 在 `src/types/api.ts` 添加 Activity、RecentRuns 类型
- [ ] 添加 StarredProject 相关类型

**Step 1.2: API 层**

- [ ] 创建 `src/api/profile.ts`
- [ ] 实现 mock 数据（后端 API 未就绪时）
- [ ] 添加 star/unstar API 调用

**Step 1.3: Store**

- [ ] 创建 `src/store/profile.ts` 管理收藏状态

### Phase 2: 组件开发 (P0)

**Step 2.1: Profile 侧边栏**

- [ ] 创建 `ProfileSidebar.tsx`
- [ ] 显示头像、用户名、邮箱
- [ ] Edit profile 按钮
- [ ] Teams 列表

**Step 2.2: 活跃度图**

- [ ] 创建 `ActivityGraph.tsx`
- [ ] 实现 GitHub 风格贡献图
- [ ] 支持 hover 显示详情
- [ ] 响应式设计

**Step 2.3: 项目卡片**

- [ ] 创建 `ProjectCard.tsx`
- [ ] 显示项目名、更新时间、runs 数量
- [ ] Star 按钮交互

**Step 2.4: Recent Runs**

- [ ] 创建 `RecentRuns.tsx`
- [ ] 列表展示最近执行
- [ ] 状态图标、时间显示

### Phase 3: Tab 页面 (P0)

**Step 3.1: ProfileTab**

- [ ] 创建 `tabs/ProfileTab.tsx`
- [ ] 组合 Projects 区域、Activity 图、Recent Runs

**Step 3.2: ProjectsTab**

- [ ] 创建 `tabs/ProjectsTab.tsx`
- [ ] Starred projects 卡片区
- [ ] Projects 表格（无 Traces 列）
- [ ] 搜索、分页功能

**Step 3.3: StarsTab**

- [ ] 创建 `tabs/StarsTab.tsx`
- [ ] 收藏项目列表视图

### Phase 4: 主页面集成 (P0)

**Step 4.1: ProfilePage**

- [ ] 创建 `ProfilePage.tsx`
- [ ] 左右布局：Sidebar + Tabs
- [ ] 路由参数处理（支持 /profile?tab=projects）

**Step 4.2: 路由更新**

- [ ] 修改 `App.tsx` 路由配置
- [ ] 更新导航链接

### Phase 5: 现有页面更新 (P1)

**Step 5.1: ProjectList 页面**

- [ ] 添加 Star 列
- [ ] Star 按钮交互
- [ ] 同步 store 状态

---

## 五、API Mock 数据（开发阶段）

由于后端 API 可能未就绪，先实现 mock 数据：

```typescript
// src/api/profile.ts
export const profileApi = {
  // Mock: 获取活跃度数据
  async getActivity(): Promise<ActivityResponse> {
    // 生成近365天的模拟数据
    const contributions = generateMockContributions();
    return { contributions, total_runs: 156, total_projects: 8 };
  },

  // Mock: 获取最近 runs
  async getRecentRuns(limit = 5): Promise<ExecutionResp[]> {
    // 从现有 executions API 获取
    const res = await executionApi.getExecutions({ size: limit });
    return res.items || [];
  },

  // Mock: 收藏功能（localStorage 暂存）
  async starProject(id: number): Promise<void> {
    const starred = JSON.parse(localStorage.getItem('starred') || '[]');
    localStorage.setItem('starred', JSON.stringify([...starred, id]));
  },

  async unstarProject(id: number): Promise<void> {
    const starred = JSON.parse(localStorage.getItem('starred') || '[]');
    localStorage.setItem(
      'starred',
      JSON.stringify(starred.filter((i) => i !== id))
    );
  },

  async getStarredProjects(): Promise<number[]> {
    return JSON.parse(localStorage.getItem('starred') || '[]');
  },
};
```

---

## 六、样式规范

### 6.1 颜色变量

```css
/* Activity Graph 颜色 */
--activity-level-0: #ebedf0; /* 无活动 */
--activity-level-1: #9be9a8; /* 1-2 次 */
--activity-level-2: #40c463; /* 3-5 次 */
--activity-level-3: #30a14e; /* 6-10 次 */
--activity-level-4: #216e39; /* >10 次 */

/* Star 颜色 */
--star-active: #f59e0b; /* 已收藏 */
--star-inactive: #d1d5db; /* 未收藏 */
```

### 6.2 响应式断点

```css
/* Mobile */
@media (max-width: 576px) {
  /* Sidebar 折叠为顶部 */
  /* Activity graph 简化显示 */
}

/* Tablet */
@media (max-width: 992px) {
  /* 2列项目卡片 */
}

/* Desktop */
@media (min-width: 993px) {
  /* 完整布局 */
}
```

---

## 七、验证清单

### 功能验证

- [ ] Profile 页面正确加载用户数据
- [ ] Tabs 切换正常工作
- [ ] Activity 图正确显示活跃度
- [ ] Star/Unstar 功能正常
- [ ] Projects 表格无 Traces 列
- [ ] 搜索、分页正常
- [ ] Recent Runs 正确显示

### 技术验证

```bash
npm run type-check   # TypeScript 检查
npm run lint         # ESLint 检查
npm run dev          # 开发服务器测试
```

### UI/UX 验证

- [ ] 响应式布局正确
- [ ] 主题色彩一致
- [ ] 加载状态显示
- [ ] 空状态处理
- [ ] 错误状态处理

---

## 八、风险与依赖

| 风险              | 影响 | 缓解方案                      |
| ----------------- | ---- | ----------------------------- |
| 后端 API 未就绪   | 中   | 使用 Mock 数据开发，后期切换  |
| Activity 数据量大 | 低   | 前端聚合，按需加载            |
| Star 状态同步     | 低   | Zustand + localStorage 持久化 |

---

## 九、里程碑

| 阶段    | 内容                         | 状态      |
| ------- | ---------------------------- | --------- |
| Phase 1 | 基础架构（类型、API、Store） | ⏳ 待开始 |
| Phase 2 | 组件开发                     | ⏳ 待开始 |
| Phase 3 | Tab 页面                     | ⏳ 待开始 |
| Phase 4 | 主页面集成                   | ⏳ 待开始 |
| Phase 5 | 现有页面更新                 | ⏳ 待开始 |

---

**等待确认后开始实施。**
