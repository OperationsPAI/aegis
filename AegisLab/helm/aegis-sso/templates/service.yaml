apiVersion: v1
kind: Service
metadata:
  name: {{ include "aegis-sso.fullname" . }}
  labels:
    {{- include "aegis-sso.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "aegis-sso.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
