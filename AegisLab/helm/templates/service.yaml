# Redis Service
{{- if eq .Values.configmap.system.env_mode "staging" }}
apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-redis
spec:
  type: NodePort
  selector:
    app: {{ .Release.Name }}-redis
  ports:
    - port: {{ .Values.servicePorts.redis | default 6379 }}
      nodePort: 32279
{{- else }}
apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-redis
spec:
  type: ClusterIP
  selector:
    app: {{ .Release.Name }}-redis
  ports:
    - port: {{ .Values.servicePorts.redis | default 6379 }}
{{- end }}

---
# MySQL Service
{{- if eq .Values.configmap.system.env_mode "staging" }}
apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-mysql
spec:
  type: NodePort
  selector:
    app: {{ .Release.Name }}-mysql
  ports:
    - port: {{ .Values.servicePorts.mysql | default 3306 }}
      targetPort: {{ .Values.servicePorts.mysql | default 3306 }}
      nodePort: 32206
{{- else }}
apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-mysql
spec:
  type: ClusterIP
  selector:
    app: {{ .Release.Name }}-mysql
  ports:
    - port: {{ .Values.servicePorts.mysql | default 3306 }}
      targetPort: {{ .Values.servicePorts.mysql | default 3306 }}
{{- end }}

---
# etcd Headless Service
apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-etcd-headless
spec:
  clusterIP: None
  selector:
    app: {{ .Release.Name }}-etcd
  ports:
    - name: client
      port: {{ .Values.servicePorts.etcd.client | default 2379 }}
      targetPort: {{ .Values.servicePorts.etcd.client | default 2379 }}
    - name: peer
      port: {{ .Values.servicePorts.etcd.peer | default 2380 }}
      targetPort: {{ .Values.servicePorts.etcd.peer | default 2380 }}

---
# etcd External Service
{{- if eq .Values.configmap.system.env_mode "staging" }}
apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-etcd-external
spec:
  type: NodePort
  selector:
    app: {{ .Release.Name }}-etcd
  ports:
    - port: {{ .Values.servicePorts.etcd.client | default 2379 }}
      targetPort: {{ .Values.servicePorts.etcd.client | default 2379 }}
      nodePort: 31379
{{- else }}
apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-etcd-external
spec:
  type: ClusterIP
  selector:
    app: {{ .Release.Name }}-etcd
  ports:
    - port: {{ .Values.servicePorts.etcd.client | default 2379 }}
      targetPort: {{ .Values.servicePorts.etcd.client | default 2379 }}
{{- end }}

---
# Aegis API (monolith) Service
apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-aegis-api
  {{- with .Values.microservices.aegisApi.service.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  {{- if eq .Values.configmap.system.env_mode "staging" }}
  type: NodePort
  {{- else }}
  type: {{ .Values.microservices.aegisApi.service.type }}
  {{- end }}
  selector:
    app: {{ .Release.Name }}-aegis-api
  ports:
    - name: http
      port: {{ .Values.microservices.aegisApi.httpPort }}
      targetPort: {{ .Values.microservices.aegisApi.httpPort }}
      {{- if or (eq .Values.configmap.system.env_mode "staging") (and (eq .Values.microservices.aegisApi.service.type "NodePort") .Values.microservices.aegisApi.service.nodePort) }}
      nodePort: {{ .Values.microservices.aegisApi.service.nodePort }}
      {{- end }}
    - name: intake-grpc
      port: {{ .Values.microservices.aegisApi.intakeGrpcPort | default 9096 }}
      targetPort: intake-grpc

---
apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-runtime-worker-service
spec:
  type: ClusterIP
  selector:
    app: {{ .Release.Name }}-runtime-worker-service
  ports:
    - name: grpc
      port: {{ .Values.microservices.runtimeWorkerService.grpcPort }}
      targetPort: {{ .Values.microservices.runtimeWorkerService.grpcPort }}
    - name: otlp
      port: {{ .Values.configmap.otlp.port }}
      targetPort: {{ .Values.configmap.otlp.port }}

---
apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-buildkit
spec:
  selector:
    app: {{ .Release.Name }}-buildkit
  ports:
    - port: {{ .Values.buildkit.port }}
      targetPort: {{ .Values.buildkit.port }}
  type: ClusterIP

{{- if .Values.loki.enabled }}
---
# Loki Service
apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-loki
spec:
  type: ClusterIP
  selector:
    app: {{ .Release.Name }}-loki
  ports:
    - name: http
      port: {{ .Values.servicePorts.loki | default 3100 }}
      targetPort: {{ .Values.servicePorts.loki | default 3100 }}
{{- end }}

{{- if .Values.prometheus.enabled }}
---
# Prometheus Service
{{- if eq .Values.configmap.system.env_mode "staging" }}
apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-prometheus
spec:
  type: NodePort
  selector:
    app: {{ .Release.Name }}-prometheus
  ports:
    - name: http
      port: {{ .Values.servicePorts.prometheus | default 9090 }}
      targetPort: {{ .Values.servicePorts.prometheus | default 9090 }}
      nodePort: 32090
{{- else }}
apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-prometheus
spec:
  type: ClusterIP
  selector:
    app: {{ .Release.Name }}-prometheus
  ports:
    - name: http
      port: {{ .Values.servicePorts.prometheus | default 9090 }}
      targetPort: {{ .Values.servicePorts.prometheus | default 9090 }}
{{- end }}
{{- end }}

{{- if .Values.microservices.notify.enabled }}
---
apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-notify
spec:
  type: {{ .Values.microservices.notify.service.type | default "ClusterIP" }}
  selector:
    app: {{ .Release.Name }}-notify
  ports:
    - name: http
      port: {{ .Values.microservices.notify.httpPort }}
      targetPort: http
      {{- if and (eq (.Values.microservices.notify.service.type | default "ClusterIP") "NodePort") .Values.microservices.notify.service.nodePort }}
      nodePort: {{ .Values.microservices.notify.service.nodePort }}
      {{- end }}
{{- end }}

{{- if .Values.microservices.blob.enabled }}
---
apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-blob
spec:
  type: {{ .Values.microservices.blob.service.type | default "ClusterIP" }}
  selector:
    app: {{ .Release.Name }}-blob
  ports:
    - name: http
      port: {{ .Values.microservices.blob.httpPort }}
      targetPort: http
      {{- if and (eq (.Values.microservices.blob.service.type | default "ClusterIP") "NodePort") .Values.microservices.blob.service.nodePort }}
      nodePort: {{ .Values.microservices.blob.service.nodePort }}
      {{- end }}
{{- end }}

{{- if .Values.microservices.gateway.enabled }}
---
apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-gateway
spec:
  type: {{ .Values.microservices.gateway.service.type | default "ClusterIP" }}
  selector:
    app: {{ .Release.Name }}-gateway
  ports:
    - name: http
      port: {{ .Values.microservices.gateway.httpPort }}
      targetPort: http
      {{- if and (eq (.Values.microservices.gateway.service.type | default "ClusterIP") "NodePort") .Values.microservices.gateway.service.nodePort }}
      nodePort: {{ .Values.microservices.gateway.service.nodePort }}
      {{- end }}
{{- end }}

{{- if .Values.microservices.configcenter.enabled }}
---
apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-configcenter
spec:
  type: ClusterIP
  selector:
    app: {{ .Release.Name }}-configcenter
  ports:
    - name: http
      port: {{ .Values.microservices.configcenter.httpPort }}
      targetPort: http
{{- end }}
---
# Aegis SSO Service
apiVersion: v1
kind: Service
metadata:
  name: {{ include "sso.fullname" . }}
  labels:
    {{- include "sso.labels" . | nindent 4 }}
spec:
  type: {{ .Values.sso.service.type }}
  selector:
    {{- include "sso.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.sso.service.port }}
      targetPort: http
      {{- if and (eq .Values.sso.service.type "NodePort") .Values.sso.service.nodePort }}
      nodePort: {{ .Values.sso.service.nodePort }}
      {{- end }}
