global:
  imageRegistry: pair-diag-cn-guangzhou.cr.volces.com
  images:
    rcabench:
      name: pair/rcabench
      pullPolicy: IfNotPresent

images:
  busybox:
    name: pair/busybox
  buildkit:
    name: pair/buildkit

harbor:
  enabled: false

initialization:
  initDrainTemplate:
    enabled: false

  initHelmCharts:
    image:
      name: pair/init-helm-charts

configmap:
  system:
    env_mode: "test"
  k8s:
    init_container:
      busybox_image: pair-diag-cn-guangzhou.cr.volces.com/pair/busybox:1.35

mysql:
  image:
    name: pair/mysql
    tag: "8.0.43"
    pullPolicy: IfNotPresent
  persistence:
    enabled: false

# redis subchart overrides (mirrors legacy images.redis; persistence disabled).
redis:
  image:
    name: pair/redis
    tag: "8.0-M02-alpine3.20"
    pullPolicy: IfNotPresent
  persistence:
    enabled: false

# etcd subchart overrides (mirrors legacy images.etcd; persistence disabled).
etcd:
  image:
    name: pair/etcd
    tag: "v3.6.7"
    pullPolicy: IfNotPresent
  persistence:
    enabled: false
